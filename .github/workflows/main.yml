name : my_first action

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name : Run a script
      env :
        HI : hello!
        WO : world!
      run : |
       echo $HI $WO
    - name : Install python
      uses : actions/setup-python@v2
      with : 
        python-version: '3.x'
    - name : Test python program
      run : |
        cd ..
        python homework.py
 
 
      
